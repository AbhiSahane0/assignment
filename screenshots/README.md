# Screenshots

Capture these six views and save them here with these exact filenames (the
main README embeds them):

| File | What to capture |
|---|---|
| `login.png` | Login page (light mode) |
| `dashboard.png` | Dashboard as Super Admin — stat tiles + charts |
| `dashboard-dark.png` | Same dashboard with dark mode toggled |
| `employees.png` | Employees table with filters visible |
| `org-chart.png` | Org chart with a couple of levels expanded |
| `profile.png` | My Profile page (log in as arjun.rao@ems.com to show the employee view) |

Tip: use a clean 1440×900 browser window, and take them after `npm run seed`
so the data looks realistic.
